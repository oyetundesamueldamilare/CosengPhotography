@page "/"

<div class="animate-fade-in">
    <div class="container-fluid p-0 bg-black text-white overflow-hidden position-relative" style="min-height: 75vh; display: flex; align-items: center;">
        <div class="row w-100 m-0 align-items-center">

            <div class="col-lg-6 p-5 ps-lg-5 text-start container-padding">
                <span class="text-uppercase tracking-widest text-muted small d-block mb-3" style="letter-spacing: 4px; color: #888 !important;">Digital Asset Management Container</span>
                <h1 class="display-3 mb-4" style="font-weight: 800; letter-spacing: -2px; line-height: 1.05;">
                    PRESERVING <br />MOMENTS, <br /><span style="font-weight: 300; color: #aaa;">SECURELY.</span>
                </h1>
                <p class="lead text-muted mb-5" style="max-width: 500px; color: #bbb !important; font-size: 1.1rem; line-height: 1.6;">
                    A decoupled client-delivery portal built for high-tier performance. Upload original photo assets, encrypt distribution spaces, and dispatch custom client pipelines seamlessly.
                </p>

                <div class="d-flex gap-3">
                    <a href="/dashboard" class="btn btn-outline-light px-4 py-3 text-uppercase font-monospace tracking-wide text-decoration-none btn-premium">
                        Launch Dashboard Context
                    </a>
                </div>
            </div>

            <div class="col-lg-6 p-5 d-none d-lg-flex justify-content-center align-items-center position-relative">
                <div class="square-graphic-wrapper">
                    <div class="border-accent"></div>
                    <div class="glass-card-preview">
                        <div class="d-flex justify-content-between mb-4">
                            <span class="badge bg-white text-dark font-monospace">ACTIVE KERNEL</span>
                            <span class="text-muted small">AES-256</span>
                        </div>
                        <h4 class="font-monospace text-white mb-1">Secure Footprint Delivery Engine</h4>
                        <p class="text-muted small mb-0">Operational dashboard mapping for client delivery configurations.</p>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <div class="container my-5 py-5">
        <div class="text-center mb-5">
            <h2 class="text-uppercase tracking-widest font-monospace h5" style="letter-spacing: 3px; font-weight: bold;">System Pipeline Core Capabilities</h2>
            <hr style="max-width: 60px; border-top: 3px solid #000; margin: 15px auto;" />
        </div>

        <div class="row g-4 text-start">
            <div class="col-md-4">
                <div class="card p-4 h-100 border-0 shadow-sm" style="border-radius: 0; background: #fff;">
                    <div class="fs-2 mb-3">📁</div>
                    <h5 style="font-weight: 700;">Decoupled Blob Storage</h5>
                    <p class="text-muted small mb-0">Streams original file byte streams straight into localized infrastructure containers with near-zero latency degradation.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-4 h-100 border-0 shadow-sm" style="border-radius: 0; background: #fff;">
                    <div class="fs-2 mb-3">🔒</div>
                    <h5 style="font-weight: 700;">Secure Pin Handshakes</h5>
                    <p class="text-muted small mb-0">Automatic generation of unique 4-digit token authenticators, completely hiding customer collections from unauthorized scrapers.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card p-4 h-100 border-0 shadow-sm" style="border-radius: 0; background: #fff;">
                    <div class="fs-2 mb-3">⚡</div>
                    <h5 style="font-weight: 700;">Blazor Render Performance</h5>
                    <p class="text-muted small mb-0">Leverages the power of client-side WebAssembly rendering or interactive server state synchronizations for lightning-fast photo loads.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .container-padding {
        padding-left: 10% !important;
    }

    .tracking-widest {
        letter-spacing: 0.25em;
    }

    .btn-premium {
        border-radius: 0;
        font-size: 13px;
        font-weight: bold;
        transition: all 0.3s ease;
    }

        .btn-premium:hover {
            background-color: #fff !important;
            color: #000 !important;
        }

    /* Abstract UI Art Elements */
    .square-graphic-wrapper {
        position: relative;
        width: 400px;
        height: 400px;
        background: linear-gradient(135deg, #111 0%, #222 100%);
        border: 1px solid #333;
    }

    .border-accent {
        position: absolute;
        top: -15px;
        right: -15px;
        width: 100%;
        height: 100%;
        border: 2px solid #fff;
        opacity: 0.15;
        z-index: 0;
    }

    .glass-card-preview {
        position: absolute;
        bottom: -30px;
        left: -30px;
        width: 350px;
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255,255,255,0.1);
        padding: 25px;
        z-index: 2;
    }

    /* Entry Animations */
    .animate-fade-in {
        animation: fadeIn 0.8s ease-out forwards;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(15px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>